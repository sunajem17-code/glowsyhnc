{
  "rules": [
    {
      "id": "@secretlint/secretlint-rule-preset-recommend"
    },
    {
      "id": "@secretlint/secretlint-rule-pattern",
      "options": {
        "patterns": [
          {
            "name": "Stripe Live Secret Key",
            "pattern": "/sk_live_[A-Za-z0-9]{24,}/",
            "message": "Stripe live secret key detected — remove it and rotate at dashboard.stripe.com/apikeys"
          },
          {
            "name": "Stripe Restricted Key",
            "pattern": "/rk_live_[A-Za-z0-9]{24,}/",
            "message": "Stripe live restricted key detected — remove it and rotate at dashboard.stripe.com/apikeys"
          },
          {
            "name": "Stripe Webhook Secret",
            "pattern": "/whsec_[A-Za-z0-9+/]{32,}/",
            "message": "Stripe webhook secret detected — remove it and rotate in your Stripe dashboard"
          },
          {
            "name": "Supabase Service Role Key",
            "pattern": "/service_role[^'\"\\s]*eyJ[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+/i",
            "message": "Supabase service role key detected — this must stay server-side only, never in source"
          }
        ]
      }
    }
  ]
}
